"""4-layer memory (Pillar 7).

- L1 working   — in-process, dies with session
- L2 phase     — SQLite, scoped to a refactor phase
- L3 project   — markdown with frontmatter, human-readable
- L4 corpus    — migration cookbook (structured lookup tables, not RAG-only)
"""
